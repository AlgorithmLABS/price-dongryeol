import styled from 'styled-components';

const Portion = styled.td`
  font-family: Roboto;
  font-size: 15px;
  font-weight: bold;
  font-stretch: normal;
  font-style: normal;
  line-height: 1.6;
  letter-spacing: normal;
  color: #0099fe;
`;

export default Portion;
