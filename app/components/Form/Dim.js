import styled from 'styled-components';

const Dim = styled.div`
  position: fixed;
  top: 0;
  bottom: 0;
  left: 0;
  right: 0;
  z-index: 999;
  display: flex;
  align-items: center;
  justify-content: center;
  background-color: rgb(71 95 123 / 30%);
`;

export default Dim;
